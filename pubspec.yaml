name: flutter_login_app
description: A clean and professional Flutter login page.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  google_sign_in: ^7.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
